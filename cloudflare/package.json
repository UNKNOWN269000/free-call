{
  "name": "freecall-cloudflare",
  "private": true,
  "version": "1.0.0",
  "description": "FreeCall signaling server for Cloudflare Workers + Durable Objects",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
